.venv
.winenv
.linuxenv
.ENV
ENV

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Trained weights and training data (phase 2 outputs)
*.npz
*.pth
*.bin
data/
weights/
# Generated quantized weights (reproduction output, not a source file)
/my_params.h
/params.h

# Reference Kaggle notebooks: large (~250 MB, embedded outputs), kept locally
# only. Notebook-dependent tests skip when these are absent.
reference/kaggle_solution/*.ipynb

# Run logs and generated features
*.log
features/
*.features

# Vendored external GPL project (the now-public original fork), built locally
kaggle-stockfish/
