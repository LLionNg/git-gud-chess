.venv
.winenv
.linuxenv
.ENV
ENV

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/

# Trained neural weights
*.npz
*.pth
*.bin
data/
weights/

# Run logs
*.log

# On-disk artifacts that must never be committed here. They exist locally only
# (the external fork clone and the large Kaggle notebooks); the full reproduction
# that uses them lives on the reference/lgeu-solution branch.
reference/
kaggle-stockfish/
